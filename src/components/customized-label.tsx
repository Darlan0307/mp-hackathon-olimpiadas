const CustomizedLabel = () => {
  return (
    <text x={0} y={0} textAnchor="middle">
      <tspan x={0} y={16} className="fill-foreground text-xl font-bold">
        Continents
      </tspan>
    </text>
  );
};

export default CustomizedLabel;
